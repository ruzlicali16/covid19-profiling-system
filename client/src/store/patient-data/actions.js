export function personalDetails(context, details) {
  context.commit("personalDetails", details);
}

export function medicalHistory(context, details) {
  context.commit("medicalHistory", details);
}

export function covidQuestionaire(context, details) {
  context.commit("covidQuestionaire", details);
}

export function healthDetails(context, details) {
  context.commit("healthDetails", details);
}
